# การใช้งาน Tokenizer ร่วมกับงานหลัก

## ภาพรวมระบบ

ระบบประกอบด้วย 2 server ที่ทำงานร่วมกัน:

### 1. Corpus Server (Port 5000) - งานหลัก
- จัดการ UI หลักสำหรับแก้ไขข้อความ
- จัดการ corpus และการบันทึกผลลัพธ์
- ใช้การเรียนรู้แบบพื้นฐาน (word-by-word)

### 2. Tokenizer Server (Port 5001) - ฟีเจอร์เสริม
- ตัดคำด้วย PyThaiNLP
- เรียนรู้คำผิดระดับ token อย่างแม่นยำ
- แก้ไขข้อความอัตโนมัติ

## การติดตั้งและรัน

### 1. ติดตั้ง Dependencies

```bash
# สำหรับ Corpus Server
pip3 install -r requirements-server.txt

# สำหรับ Tokenizer Server  
pip3 install -r requirements-tokenizer.txt
```

### 2. รัน Servers

```bash
# Terminal 1: รัน Corpus Server
python3 corpus-server.py

# Terminal 2: รัน Tokenizer Server
python3 tokenizer-server.py
```

### 3. เข้าใช้งาน

- **งานหลัก**: http://localhost:5000
- **ทดสอบ Integration**: http://localhost:5000/test-integration.html

## ฟีเจอร์ใหม่ใน UI หลัก

เมื่อ Tokenizer Server ทำงาน จะมีฟีเจอร์เพิ่มเติม:

### 🔤 ปุ่ม "ตัดคำ"
- แสดงผลการตัดคำของข้อความปัจจุบัน
- ใช้ PyThaiNLP engine='newmm'

### 🧠 ปุ่ม "เรียนรู้อัจฉริยะ"
- เปรียบเทียบข้อความต้นฉบับกับที่แก้ไข
- หาความแตกต่างระดับ token
- เพิ่มคำผิดใหม่ใน corpus อัตโนมัติ

### ℹ️ แสดงสถานะ Smart Learning
- แสดงข้อความแจ้งเตือนเมื่อ Tokenizer Server พร้อมใช้งาน

## การทำงานร่วมกัน

### การเรียนรู้แบบอัจฉริยะ
เมื่อกดปุ่ม "ยอมรับ" ระบบจะ:

1. **ถ้ามี Tokenizer Server**: ใช้ Smart Learning
   - ตัดคำทั้งข้อความต้นฉบับและที่แก้ไข
   - เปรียบเทียบระดับ token
   - เรียนรู้ความแตกต่างที่แม่นยำ

2. **ถ้าไม่มี Tokenizer Server**: ใช้ Basic Learning
   - เปรียบเทียบแบบ word-by-word
   - เรียนรู้ตามวิธีเดิม

### การแชร์ Corpus
- ทั้งสอง server ใช้ไฟล์ `main-corpus.json` ร่วมกัน
- การเรียนรู้จาก Tokenizer จะอัปเดต corpus หลัก
- UI หลักจะใช้ corpus ที่อัปเดตแล้ว

## การทดสอบ

### 1. ทดสอบแยกส่วน
```bash
# ทดสอบ Tokenizer Server
python3 test-tokenizer.py

# ทดสอบ Corpus Server (เปิด browser ไปที่ localhost:5000)
```

### 2. ทดสอบการทำงานร่วมกัน
```bash
# เปิด browser ไปที่
http://localhost:5000/test-integration.html
```

## ข้อดีของการใช้ร่วมกัน

### ✅ ไม่กระทบงานเดิม
- งานหลักทำงานได้ปกติแม้ไม่มี Tokenizer Server
- ฟีเจอร์ใหม่จะปรากฏเฉพาะเมื่อ Tokenizer Server พร้อมใช้งาน

### ✅ การเรียนรู้ที่แม่นยำขึ้น
- ใช้ PyThaiNLP ตัดคำอย่างถูกต้อง
- เรียนรู้ความแตกต่างระดับ token แทนระดับคำ
- ลดการเรียนรู้ผิดจากการตัดคำไม่ถูกต้อง

### ✅ ประสิทธิภาพดีขึ้น
- Tokenizer Server ทำงานแยกจาก UI
- ไม่ทำให้ UI ช้าลง
- สามารถปิด/เปิด Tokenizer Server ได้ตามต้องการ

## การแก้ไขปัญหา

### Tokenizer Server ไม่ทำงาน
- ตรวจสอบว่าติดตั้ง pythainlp แล้ว
- ตรวจสอบ port 5001 ว่าง
- ดู log ใน terminal

### ไม่เห็นปุ่ม Tokenizer ใน UI
- ตรวจสอบว่า Tokenizer Server ทำงานที่ port 5001
- Refresh หน้าเว็บ
- ดู Console ใน Developer Tools